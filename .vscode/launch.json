{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "pnpm dev",
      "name": "Development server (do not combine with pnpm ensure-dev)",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
