{
  "install": "corepack enable && pnpm install --frozen-lockfile",
  "terminals": [
    {
      "name": "dev",
      "command": "pnpm dev",
      "description": "Foreground Astro dev server (tmux). Do not also run pnpm dev:background on port 4321."
    }
  ]
}
