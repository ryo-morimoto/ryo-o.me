{
  "install": "corepack enable && pnpm install --frozen-lockfile",
  "terminals": [
    {
      "name": "dev",
      "command": "pnpm dev:status >/dev/null 2>&1 || pnpm dev:background; pnpm dev:logs",
      "description": "Same as AGENTS.md: ensure Astro is up on http://localhost:4321, then stream logs"
    }
  ]
}
