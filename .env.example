# Local `pnpm dev` / `pnpm build` do not require any secrets.
# Copy to `.env` only if you add public Vite/Astro vars later.
#
# Cloudflare D1/KV in wrangler.jsonc use placeholder IDs.
# Quiet Letter uses in-memory storage when worker bindings are empty.
